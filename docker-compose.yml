services:
  web:
    container_name: porto
    image: dickyjoelsaputra/porto:latest
    expose:
      - "8080"
    networks:
      - shared_network
    restart: unless-stopped

networks:
  shared_network:
    external: true